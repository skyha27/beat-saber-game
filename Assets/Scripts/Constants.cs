using UnityEngine;

public static class Constants
{
    // Beat movement
    public const float BEAT_SPEED = -5f;

    public const float BEAT_SPWAN_INVERSE_DELAY = 2.2f;

    public const float SABER_SPEED_MULTIPLIER = 1.5f;
}
